{% extends "layout.html" %}
{% block content %}
<!doctype html>
<title>Hello form Falsk</title>
{% if result %}
  <h1>{{ result }}</h1>
{% else %}
  <h1>Hello World!</h1>
{% endif %}

<form action="/post" method="post" class="form-inline">
  <label for="ip_address">ServerIP</label>
  <input type="text" class="form-control" id="name" name="name" placeholder="IP Address">
  <button type="submit" class="btn btn-default">submit</button>
</form>
{% endblock %}
